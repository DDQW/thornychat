//! Autostart registration via `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`,
//! toggled from Settings. The app supports a `--minimized` launch flag so
//! autostart doesn't pop a window on login. Phase 7.
