//! System tray icon + native menu via the `tray-icon` crate, with an
//! unread-count badge. Phase 7.

#[derive(Debug, Clone, Default)]
pub struct State;

#[derive(Debug, Clone)]
pub enum Message {}
